{
  "name": "kerala-play",
  "version": "1.0.2",
  "private": true,
  "type": "module",
  "description": "Kerala Play persistent social world",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "prestart": "node scripts/patch-runtime.mjs",
    "start": "node public-server.mjs",
    "start:production": "node production-server.mjs",
    "production:check": "node scripts/check-production-env.mjs",
    "production:import-local": "node scripts/import-local-game.mjs",
    "test": "node --test server.test.mjs world-alerts.test.mjs recognition.test.mjs leaderboards.test.mjs safe-driving.test.mjs",
    "premobile:prepare": "node scripts/patch-runtime.mjs",
    "mobile:prepare": "node scripts/prepare-mobile.mjs",
    "mobile:add": "npm run mobile:prepare && npx cap add android && npm run mobile:configure",
    "mobile:sync": "npm run mobile:prepare && npx cap sync android && npm run mobile:configure",
    "mobile:open": "npm run mobile:sync && npx cap open android",
    "mobile:release:check": "node scripts/check-mobile-release.mjs",
    "mobile:configure": "npm run mobile:landscape && npm run mobile:version && npm run mobile:branding",
    "mobile:landscape": "node scripts/lock-android-landscape.mjs",
    "mobile:version": "node scripts/set-android-version.mjs",
    "mobile:branding": "node scripts/set-android-branding.mjs"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
